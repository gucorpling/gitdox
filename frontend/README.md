# GitDOX front end

This directory contains the GitDOX js frontend application. Source code for the frontend is in src/.

For python backend code, see ../backend/. Schema files for the exporter live in ../schemas/